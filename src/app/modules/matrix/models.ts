export type MatrixResult = {
  fX: number;
  fMax: number;
  closingCompetencies: boolean;
  creditUnits: boolean;
  matrix: number[][]
}

export type PopulationResult = {
  resultMatrixArray:MatrixResult[];
};


